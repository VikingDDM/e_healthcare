@model IEnumerable<E_health_care.tblDiesease>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_DoctorLayout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.strDiseasedName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.strDiseasedDetails)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.strDiseasedName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.strDiseasedDetails)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.intDiseaseID }) |
            @Html.ActionLink("Details", "Details", new { id=item.intDiseaseID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.intDiseaseID })
        </td>
    </tr>
}

</table>
